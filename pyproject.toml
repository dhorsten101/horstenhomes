[project]
name = "horstenhomes"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "Django>=5.0",
  "psycopg[binary]>=3.1",
  "django-tenants>=3.7",
  "celery>=5.3",
  "redis>=5.0",
  "gunicorn>=21.2",
  "python-dotenv>=1.0",
]
